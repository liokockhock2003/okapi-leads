# Rebuilt inside the image / not wanted in the build context.
/vendor
/node_modules
/.git
.env
# Keep the host's local log history out of the image (the dir itself stays,
# preserved by storage/logs/.gitignore).
/storage/logs/*.log
